# Planning Notes

Historical plan documents in this directory may still reference the old Scala package
base `io.github.eleven19.mill.interceptor` and the old Maven group
`io.github.eleven19.mill-interceptor`.

The active canonical names are:

- Scala package base: `io.eleven19.mill.interceptor`
- Maven group: `io.eleven19.mill-interceptor`
